# countered


Added a different logic to the counter. You call the counter program using the public function `countered` along with an integer number.
If the integer is odd, then increment it by 2 else decrement by 3. You can `read the latest counter` value and also `reset` it at any point
